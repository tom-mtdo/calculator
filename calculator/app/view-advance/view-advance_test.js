'use strict';

describe('myApp.viewAdvance module', function() {

  beforeEach(module('myApp.viewAdvance'));

  describe('view advance controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewAdvanceCtrl = $controller('ViewAdvanceCtrl');
      expect(viewAdvanceCtrl).toBeDefined();
    }));

  });
});